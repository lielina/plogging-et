import React from 'react';
import BlogPost from './BlogPost';

// Simple test component to verify the file can be imported
const BlogPostTest: React.FC = () => {
  return (
    <div>
      <h1>BlogPost Test Component</h1>
      <p>This is a simple test to verify the BlogPost component can be imported.</p>
    </div>
  );
};

export default BlogPostTest;
